export enum PricingType {
  PerPerson = "perPerson",
  PerGroup = "perGroup",
}

export enum BookingMode {
  Automatic = "automatic",
  Manual = "manual",
}

export enum RestrictionMode {
  ByPeople = "people",
  ByBookings = "bookings",
}
